# 后续工作

当前代码、自动化测试、前端生产构建和 Windows desktop 打包均已完成。后续重点是连接真实服务后的人工验收与发布准备。

## 人工验收

1. 使用真实模型分别创建长篇和短篇项目，确认短篇进入 `architect_short` 且不会生成卷/弧分层。
2. 使用一部长篇和一篇短篇测试拆文，覆盖黄金三章放行、灰区形态裁定、失败章节重试和断点恢复。
3. 使用粘贴文本、单文件和目录三种来源测试扫榜，覆盖更换数据源、仅凭快照恢复和超长输入分块。
4. 使用实际图片服务测试同步与异步接口，确认任务完成后能下载、保存并刷新封面。
5. 对生成章节执行去 AI 味技能，检查机械规则结果、编辑器上下文注入和章节改写闭环。

## 发布准备

- 更新版本号与发布说明。
- 需要安装包时运行 `bash scripts/build-desktop.sh --nsis`，并在干净 Windows 环境验证安装、升级和卸载。
- 发布前再次运行 `go test ./...`、`go vet ./...`、前端构建和 desktop 打包。
